/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// const dave = { firstName: 'David', lastName: 'Thomas' };
// var x = getValue(dave, 'firstName'); // -> "David"
// var y = getValue(dave, 'lastName'); // -> "Thomas"
